-- Active: 1657835453971@@localhost@3306@vscodedb

SELECT EMP.EMP_NUM, EMP_LNAME, EMP_FNAME, SAL_FROM, SAL_END, SAL_AMOUNT
    FROM LGEMPLOYEE EMP JOIN LGSALARY_HISTORY AS S ON EMP.EMP_NUM = S.EMP_NUM
    WHERE EMP.EMP_NUM IN (83731, 83745, 84039)
    ORDER BY EMP.EMP_NUM AND SAL_FROM;