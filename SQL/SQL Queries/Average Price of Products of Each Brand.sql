-- Active: 1658408076950@@127.0.0.1@3306

SELECT B.BRAND_ID, BRAND_NAME, ROUND(AVG(PROD_PRICE), 2)
    FROM LGBRAND AS B JOIN LGPRODUCT AS P ON B.BRAND_ID = P.BRAND_ID
    GROUP BY BRAND_NAME
    ORDER BY BRAND_NAME;